services:
  - type: web
    name: quantum-pixel
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: uv sync --locked --no-dev && uv cache prune --ci
    startCommand: uv run -- uvicorn quantum_pixel.web:app --host 0.0.0.0 --port $PORT